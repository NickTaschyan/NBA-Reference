import React from 'react';

const SelectionInfo = ( { selectedImg, selectedImg2}) => {


    return (
        <div>
            <p>{selectedImg} {selectedImg2}</p>
        </div>
    )
}

export default SelectionInfo;
